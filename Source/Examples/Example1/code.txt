class XXX
{
	string text;
	datetime time;

	int[] nums;
}

(XXX[] list, XXX content, string key)
{
	content.text = "";

	[open(key)]
	foreach(regexp("(?<word>\\w+)"))
	{
		list[] = XXX(text=word,time=(datetime)"1/2/2010");

		content.text = content.text + " " + word;
	}

	content.nums[]=0;
	content.nums[]=1;
	content.nums[]=2;
	content.nums[]=3;
}
